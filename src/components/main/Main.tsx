import React from "react";

const Main: React.FC = () => {

    return(
        <div> 
        <button onClick={()=>{}} >
          Start Quiz
        </button>        </div>
    )
}

export default Main;